### `Cell.cs`
- This script defines a `Cell` class, which represents a single cell in a grid-based environment.
- It has one public boolean variable `isWater`, indicating whether the cell is water or not.
- The constructor `Cell(bool isWater)` initializes the `isWater` variable of the cell.
